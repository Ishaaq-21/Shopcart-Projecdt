import React from "react";

const CheckoutBtn = () => {
  return (
    <button className="bg-shop-primary hover:bg-shop-orange hover-effect w-full rounded-full py-2 text-lg text-white font-semibold">
      Proceed to Checkout
    </button>
  );
};

export default CheckoutBtn;
